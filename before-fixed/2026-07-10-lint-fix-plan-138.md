# Lint fix plan — issue #138: `revive`

**Generated:** 2026-07-10
**Source report:** `golangci/linter-report/golangci-default/2026-07-10_15-44-41.json`

---

## 1. Issue summary

| Field | Value |
|---|---|
| Issue number | 138 |
| Linter | `revive` |
| File | `frontiir/api/controllers/analytics_controller.go` |
| Line | 1 |
| Column | 1 |
| Message | package-comments: should have a package comment |

---

## 2. Original code context

Line 1 (±10) — marker `▶` = flagged line.

```go
▶    1  package controllers
     2  
     3  import (
     4  	"bufio"
     5  	"encoding/json"
     6  	"fmt"
     7  	"net/http"
     8  	"os"
     9  	"path/filepath"
    10  	"sort"
    11  	"strconv"
```

**Flagged source line:**

```go
package controllers
```

---

## 3. Root cause analysis

**revive rule:** `package-comments: should have a package comment` — Go convention (Effective Go / revive ruleset) နှင့် မကိုက်ညီသော pattern တွေ့ရှိသည်။

---

## 4. Fix strategy

revive rule document ကို ကြည့်ပြီး project convention နှင့် ကိုက်ညီသော fix ရွေးချယ်မည်။ Cognitive complexity rule ဆိုရင် logic ကို helper function တွေ ခွဲမည်။

```go
// cognitive-complexity: extract complex conditions
func isValidState(s State) bool {
    return s == Active || s == Pending
}

// unused-parameter: use _ for intentionally unused params
func Handler(_ *gin.Context) { /* ... */ }
```

---

## 5. Before → After preview

> Code ကို modify မလုပ်ရသေးပါ။ ဒီ section သည် proposed change ၏ preview သာ ဖြစ်သည်။

### Before

```go
package controllers
```

### After

```go
// cognitive-complexity: extract complex conditions
func isValidState(s State) bool {
    return s == Active || s == Pending
}

// unused-parameter: use _ for intentionally unused params
func Handler(_ *gin.Context) { /* ... */ }
```

---

## 6. Possible side effects

* Style change — logic မပြောင်း
* Exported API rename ဆိုရင် breaking change ဖြစ်နိုင် — callers စစ်ဆေးပါ

---

## 7. Impact analysis

**Affected file:** `frontiir/api/controllers/analytics_controller.go`
**Affected package:** `frontiir/api/controllers`
**Base symbol:** `analytics_controller`

**Callers / references found:**

* No external callers found — change is local to `frontiir/api/controllers`

> Verify caller behavior does not change after the fix.

---

## 8. Recommended tests

Priority order:

```bash
# 1. Same package
go test ./frontiir/api/controllers/...
make test-unit TEST_FUNC=ALL

# 2. Full lint re-check — confirm issue #138 is resolved
GOROOT=/usr/local/go golangci-lint run --config .golangci.yml ./frontiir/api/controllers/...

# 3. Full test suite
make test-unit TEST_FUNC=ALL
```

---

## 9. Acceptance criteria

- [ ] Issue #138 (`revive` at `frontiir/api/controllers/analytics_controller.go:1`) lint error မတွေ့တော့ပါ
- [ ] `golangci-lint run` မှာ new issue မတိုးပါ
- [ ] `make test-unit TEST_FUNC=ALL` pass ဖြစ်ပါသည်
- [ ] Business logic ပြောင်းလဲမှု မရှိပါ
- [ ] `//nolint` directive မပါဝင်ပါ

---

## Important rules

> ဤ plan file သည် analysis document သာ ဖြစ်သည်။
>
> * Source code ကို modify မလုပ်ရ
> * Git state မပြောင်းရ
> * Lint issue ကို `//nolint` ဖြင့် suppress မလုပ်ရ
